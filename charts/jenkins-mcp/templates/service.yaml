apiVersion: v1
kind: Service
metadata:
  name: {{ include "jenkins-mcp.fullname" . }}
  labels:
    {{- include "jenkins-mcp.labels" . | nindent 4 }}
  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  {{- with .Values.service.sessionAffinity }}
  sessionAffinity: {{ . }}
  {{- end }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "jenkins-mcp.selectorLabels" . | nindent 4 }}
